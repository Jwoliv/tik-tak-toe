const GameBoardButton = ({ text, style }) => {
    return <button className={`w-30 rounded-lg px-5 py-2 cursor-pointer text-sm leading-tight ${style}`}>{text}</button>
}

export default GameBoardButton
