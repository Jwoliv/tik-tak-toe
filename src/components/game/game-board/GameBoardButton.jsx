const GameBoardButton = ({ text, className }) => {
    return <button className={`w-30 rounded-lg px-5 py-2 cursor-pointer text-sm leading-tight ${className}`}>{text}</button>
}

export default GameBoardButton
